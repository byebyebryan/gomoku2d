[package]
name = "gomoku-analysis"
version = "0.1.0"
edition = "2021"

[dependencies]
gomoku-core = { path = "../gomoku-core" }
gomoku-bot = { path = "../gomoku-bot" }
serde = { version = "1", features = ["derive"] }
