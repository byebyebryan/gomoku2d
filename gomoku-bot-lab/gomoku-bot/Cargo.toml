[package]
name = "gomoku-bot"
version = "0.1.0"
edition = "2021"

[dependencies]
gomoku-core = { path = "../gomoku-core" }
rand = "0.10"
serde_json = "1"
instant = { version = "0.1", features = ["wasm-bindgen"] }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "search_perf"
harness = false
